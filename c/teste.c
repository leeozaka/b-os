#include <stdio.h>
#include <string.h>
#define TF 100

char p1 (char &elem[])
{
    //elem+=3;
    //return elem;
}
char p2 (char &elem[], int TL)
{
    int i;
    for (i=0; i<
}

int main()
{
    char frase[TF], frase2[];
    int i, TL;
    scanf("%s",&frase);
    for(i=0;i<TF||frase[i]=='\0';i++) if (frase[i]=='\0') TL=i;
    for (i=0; i<TL);i++)
    {
        frase[i]=elem(frase[i]);
    }
    //stg2
    
    feras
}