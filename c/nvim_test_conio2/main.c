#include <conio3.h>
#include <stdio.h>

int main() {
  textbackground(ROSA);
  textcolor(VERMELHO);
  // system("cls");
  gotoxy(12, 12);
  printf("Hello World! %s", "hehe");
  textcolor(BRANCO);
  int x = 10;
  // for (int i = 0; i < 10; i++)
  printf("\n");
}
